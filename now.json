{
    "version": 2,
   "name": "airbnb",
   "builds": [{ "src": "airbnb/package.json", "use": "@now/static-build", "config": {"distDir": "build"} }],
    "routes": [
        { "src": "/(.*)", "dest": "airbnb/$1" }
    ]
}

